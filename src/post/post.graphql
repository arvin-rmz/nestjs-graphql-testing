type Post {
  id: ID!
  title: String!
  content: String!
  user: User!
}

input PostCreateInput {
  title: String!
  content: String!
}

type UserErrors {
  message: String!
}

type PostPayload {
  userErrors: [UserErrors!]!
  post: Post
}

type Query {
  post(id: ID): Post
  posts: [Post!]!
}

type Mutation {
  postCreate(postCreateInput: PostCreateInput!): PostPayload!
}
